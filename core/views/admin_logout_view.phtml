<?php
    $current_user_id = intval(getParameter('session', 'current_user'));
    $current_user = new User($current_user_id);
?>

<div>
    <form action="../core/controllers/logout.php">
        <?php echo "Welcome Back " .  $current_user->getFirstName() . " " . $current_user->getLastName() ." "; ?>
        <button type="submit">Logout</button>
    </form>
</div>